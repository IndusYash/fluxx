import React from 'react';

// You can define specific props here if the HeroSection component ever needs them.
// For now, it doesn't take any props, so the interface is empty.
export interface HeroSectionProps { }