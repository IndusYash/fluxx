const fs = require('fs'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(function(file) { file = dir + '/' + file; const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if (file.endsWith('.tsx') || file.endsWith('.ts') || file.endsWith('.css')) results.push(file); } }); return results; } const files = walk('./src'); files.forEach(file => { let content = fs.readFileSync(file, 'utf8'); let original = content; content = content.replace(/#10b981/gi, '#E5E5E5'); content = content.replace(/#059669/gi, '#D4D4D4'); content = content.replace(/rgba\(16,\s*185,\s*129,/gi, 'rgba(229, 229, 229,'); content = content.replace(/rgba\(29,\s*185,\s*84,/gi, 'rgba(229, 229, 229,'); content = content.replace(/rgba\(108,\s*255,\s*247,/gi, 'rgba(255, 255, 255,'); content = content.replace(/#6CFFF7/gi, '#FFFFFF'); if (content !== original) { fs.writeFileSync(file, content, 'utf8'); console.log('Updated ' + file); } }); console.log('Done RGB replacement.');
